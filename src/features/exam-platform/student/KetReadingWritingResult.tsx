import { CheckCircle2, ChevronLeft, ChevronRight, Eye, FileClock, Home, LoaderCircle, RotateCcw, Trophy, XCircle } from 'lucide-react';
import { useMemo, useState, type ReactNode } from 'react';
import { examPartUnits } from '../examStructure';
import type { ExamAttemptReview, ExamCompletedAttempt, ExamPartContent, ExamPlayableSet, ExamQuestion, ExamQuestionResult } from '../types';
import ExamImageViewer from './ExamImageViewer';

type State = 'correct' | 'incorrect' | 'unanswered' | 'writing';
const answerText = (value: string | string[] | undefined) => Array.isArray(value) ? value.join(', ') : String(value || '');
const stateOf = (result?: ExamQuestionResult): State => result?.type === 'long-writing' ? 'writing' : !result || result.unanswered ? 'unanswered' : result.correct ? 'correct' : 'incorrect';
const stateClass = (state: State) => state === 'correct' ? 'border-emerald-300 bg-emerald-50 text-emerald-950' : state === 'incorrect' ? 'border-rose-300 bg-rose-50 text-rose-950' : state === 'unanswered' ? 'border-amber-300 bg-amber-50 text-amber-950' : 'border-violet-300 bg-violet-50 text-violet-950';

function StateIcon({ state }: { state: State }) {
  if (state === 'correct') return <CheckCircle2 size={18} className="text-emerald-700" />;
  if (state === 'incorrect') return <XCircle size={18} className="text-rose-700" />;
  if (state === 'writing') return <span className="flex h-[18px] w-[18px] items-center justify-center rounded-full bg-violet-700 text-[10px] font-black text-white">W</span>;
  return <span className="flex h-[18px] w-[18px] items-center justify-center rounded-full border-2 border-amber-600 text-xs font-black text-amber-700">–</span>;
}

function Examples({ unit }: { unit: ExamPartContent }) {
  if (!unit.examples?.length) return null;
  return <div className="rounded-2xl border border-indigo-200 bg-indigo-50 p-4 text-sm"><p className="text-xs font-black uppercase text-indigo-800">{unit.examples.length > 1 ? 'Examples' : 'Example'}</p>{unit.examples.map((example, index) => <p key={index} className="mt-2 flex flex-wrap justify-between gap-3 font-semibold text-slate-800"><span>{example.prompt}</span><b className="border-b-2 border-dotted border-indigo-500 bg-white px-3 text-indigo-900">{example.answer}</b></p>)}</div>;
}

function TextSource({ passage, label = 'Nội dung đề' }: { passage?: string; label?: string }) {
  if (!passage?.trim()) return null;
  return <section className="rounded-2xl border border-indigo-200 bg-indigo-50 p-4 sm:p-5" data-ket-result-text-source><p className="text-xs font-black uppercase tracking-wide text-indigo-800">{label}</p><div className="mt-3 whitespace-pre-wrap text-sm font-semibold leading-7 text-slate-900">{passage}</div></section>;
}

function TwoColumn({ imageUrl, children }: { imageUrl?: string; children: ReactNode }) {
  return <div className="grid gap-5 lg:grid-cols-[minmax(0,44%)_minmax(0,56%)]"><div>{imageUrl ? <ExamImageViewer src={imageUrl} alt="Ảnh kết quả KET Reading & Writing" maxHeight="min(66vh,650px)" className="border border-slate-200 bg-white" /> : <div className="rounded-2xl border-2 border-dashed border-slate-300 p-10 text-center font-bold text-slate-600">Không có ảnh.</div>}</div><div className="min-w-0 space-y-4">{children}</div></div>;
}

function TopImage({ imageUrl }: { imageUrl?: string }) {
  return <div className="mx-auto w-full max-w-4xl">{imageUrl ? <ExamImageViewer src={imageUrl} alt="Ảnh kết quả KET Part 2" maxHeight="min(48vh,520px)" className="border border-slate-200 bg-white" /> : <div className="rounded-2xl border-2 border-dashed border-slate-300 p-10 text-center font-bold text-slate-600">Không có ảnh.</div>}</div>;
}

function ResultCard({ question, result, showPrompt = true }: { key?: string; question: ExamQuestion; result: ExamQuestionResult; showPrompt?: boolean }) {
  const state = stateOf(result);
  return <article className={`rounded-xl border-2 p-3 ${stateClass(state)}`}><p className="flex items-start gap-2 font-black"><StateIcon state={state} /><span>{question.displayNumber || question.number}.{showPrompt ? ` ${question.prompt}` : ''}</span></p><p className="mt-2 text-sm">Bạn trả lời: {question.answerPrefix && <span className="mr-1 rounded bg-slate-200 px-1.5 font-black text-slate-900">{question.answerPrefix}</span>}<b>{answerText(result.userAnswer) || 'Bỏ trống'}</b></p>{state !== 'correct' && state !== 'writing' && <p className="mt-1 text-sm font-black text-emerald-900">Đúng: {question.answerPrefix && <span className="mr-1 rounded bg-emerald-100 px-1.5">{question.answerPrefix}</span>}{answerText(result.correctAnswer)}</p>}</article>;
}

function ChoiceResult({ question, result, showPrompt }: { key?: string; question: ExamQuestion; result: ExamQuestionResult; showPrompt: boolean }) {
  const state = stateOf(result);
  const user = answerText(result.userAnswer).trim().toLocaleLowerCase('en');
  const correct = answerText(result.correctAnswer).trim().toLocaleLowerCase('en');
  return <article className={`rounded-xl border-2 p-3 ${stateClass(state)}`}><p className="flex items-start gap-2 font-black"><StateIcon state={state} /><span>{question.displayNumber || question.number}.{showPrompt ? ` ${question.prompt}` : ''}</span></p><div className="mt-3 grid grid-cols-3 gap-2">{question.options.slice(0, 3).map((option, index) => { const values = [option.label, option.text].map(value => value.trim().toLocaleLowerCase('en')); const selected = values.includes(user); const right = values.includes(correct); return <div key={option.id} className={`rounded-lg border-2 bg-white p-2 text-xs font-semibold ${right ? 'border-emerald-600' : selected ? 'border-rose-600' : 'border-slate-200'}`}><b>{String.fromCharCode(65 + index)}.</b> {option.text}{right && <CheckCircle2 size={14} className="ml-1 inline text-emerald-700" />}{selected && !right && <XCircle size={14} className="ml-1 inline text-rose-700" />}</div>; })}</div></article>;
}

function ChoicePart({ unit, results, showPrompt = false, imageTop = false, withoutImage = false, hideExamples = false }: { unit: ExamPartContent; results: ExamQuestionResult[]; showPrompt?: boolean; imageTop?: boolean; withoutImage?: boolean; hideExamples?: boolean }) {
  const body = <>{!hideExamples && <Examples unit={unit} />}<div className="space-y-2">{unit.questions.map(question => { const result = results.find(item => item.questionId === question.id); return result ? <ChoiceResult key={question.id} question={question} result={result} showPrompt={showPrompt} /> : null; })}</div></>;
  return withoutImage ? <div className="mx-auto max-w-5xl space-y-4">{body}</div> : imageTop ? <div className="space-y-5"><TopImage imageUrl={unit.imageUrl} />{body}</div> : <TwoColumn imageUrl={unit.imageUrl}>{body}</TwoColumn>;
}

function LetterPart({ unit, results }: { unit: ExamPartContent; results: ExamQuestionResult[] }) {
  const middle = unit.readingScenes?.[0]?.imageUrl;
  return <div className="overflow-x-auto rounded-2xl bg-slate-50 p-2"><div className="grid h-[clamp(360px,56vh,600px)] min-w-[900px] grid-cols-[minmax(290px,1fr)_minmax(290px,1fr)_210px] overflow-hidden rounded-2xl border-2 border-slate-300 bg-white divide-x-2 divide-slate-300" data-ket-letter-result-frames><div className="min-w-0 overflow-hidden p-2">{unit.imageUrl && <ExamImageViewer src={unit.imageUrl} alt="Lựa chọn A-H" fillFrame className="rounded-xl bg-white" />}</div><div className="min-w-0 overflow-hidden p-2">{middle && <ExamImageViewer src={middle} alt="Trang câu hỏi" fillFrame className="rounded-xl bg-white" />}</div><div className="space-y-2 overflow-y-auto p-3">{unit.questions.map(question => { const result = results.find(item => item.questionId === question.id); if (!result) return null; const state = stateOf(result); return <article key={question.id} className={`rounded-xl border-2 p-2 text-sm ${stateClass(state)}`}><p className="flex items-center gap-2 font-black"><StateIcon state={state} />{question.displayNumber || question.number}. <span className="ml-auto rounded-lg bg-white px-3 py-1">{answerText(result.userAnswer) || '—'}</span></p>{state !== 'correct' && <p className="mt-1 text-right text-xs font-black text-emerald-900">→ {answerText(result.correctAnswer)}</p>}</article>; })}</div></div></div>;
}

function TextPart({ unit, results, showPrompt = true, split = false }: { unit: ExamPartContent; results: ExamQuestionResult[]; showPrompt?: boolean; split?: boolean }) {
  const midpoint = Math.ceil(unit.questions.length / 2);
  const groups = split ? [unit.questions.slice(0, midpoint), unit.questions.slice(midpoint)] : [unit.questions];
  return <div className="space-y-5"><TextSource passage={unit.passage} label={unit.part === 7 ? 'Bài đọc, hướng dẫn và example' : 'Hướng dẫn và example'} /><div className={split ? 'grid gap-3 md:grid-cols-2' : 'space-y-2'}>{groups.map((questions, index) => <div key={index} className="space-y-2">{questions.map(question => { const result = results.find(item => item.questionId === question.id); return result ? <ResultCard key={question.id} question={question} result={result} showPrompt={showPrompt} /> : null; })}</div>)}</div></div>;
}

function WritingPart({ unit, results }: { unit: ExamPartContent; results: ExamQuestionResult[] }) {
  const question = unit.questions[0];
  const result = results.find(item => item.questionId === question?.id);
  if (!question || !result) return null;
  return <div className="space-y-5"><TextSource passage={unit.passage} label="Câu hỏi và gợi ý" /><section className="rounded-2xl border border-indigo-200 bg-indigo-50 p-4 text-sm font-bold text-indigo-950">{question.prompt}</section><article className="rounded-2xl border border-violet-300 bg-violet-50 p-5"><div className="flex flex-wrap items-center justify-between gap-3"><p className="font-black text-violet-950">Bài viết của học sinh</p><p className="rounded-xl bg-violet-800 px-4 py-2 text-lg font-black text-white">{result.writingScore ?? result.pointsAwarded}/10</p></div><div className="mt-4 min-h-40 whitespace-pre-wrap rounded-xl border border-slate-200 bg-white p-4 text-sm font-semibold leading-7 text-slate-900">{answerText(result.userAnswer) || 'Bỏ trống'}</div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-white p-3"><p className="text-xs font-black uppercase text-slate-600">Số câu</p><p className="mt-1 text-lg font-black text-slate-950">{result.sentenceCount ?? '—'}</p></div><div className="rounded-xl bg-white p-3"><p className="text-xs font-black uppercase text-slate-600">Giới hạn từ</p><p className="mt-1 text-lg font-black text-slate-950">{question.minWords}–{question.maxWords}</p></div></div>{result.grammarErrors?.length ? <div className="mt-4 rounded-xl border border-rose-200 bg-rose-50 p-3"><p className="text-xs font-black uppercase text-rose-800">Lỗi ngữ pháp</p><ul className="mt-2 list-disc space-y-1 pl-5 text-sm font-semibold text-rose-950">{result.grammarErrors.map((error, index) => <li key={index}>{error}</li>)}</ul></div> : null}{result.vocabularyErrors?.length ? <div className="mt-3 rounded-xl border border-amber-200 bg-amber-50 p-3"><p className="text-xs font-black uppercase text-amber-900">Lỗi từ vựng</p><ul className="mt-2 list-disc space-y-1 pl-5 text-sm font-semibold text-amber-950">{result.vocabularyErrors.map((error, index) => <li key={index}>{error}</li>)}</ul></div> : null}<div className="mt-3 rounded-xl border border-emerald-200 bg-emerald-50 p-3"><p className="text-xs font-black uppercase text-emerald-900">Nhận xét</p><p className="mt-2 whitespace-pre-wrap text-sm font-semibold leading-6 text-emerald-950">{result.aiFeedback || 'Giáo viên đã chấm bài.'}</p></div></article></div>;
}

function CompoundReview({ units, results }: { units: ExamPartContent[]; results: ExamQuestionResult[] }) {
  const [activeGroup, setActiveGroup] = useState(0);
  const unit = units[activeGroup] || units[0];
  if (!unit) return null;
  return <div className="space-y-4" data-ket-part-three-result data-active-group={activeGroup === 0 ? '3A' : '3B'}><nav className="flex justify-center gap-2">{units.slice(0, 2).map((candidate, index) => <button key={candidate.id} type="button" onClick={() => setActiveGroup(index)} className={`rounded-full border-2 px-5 py-2 text-sm font-black ${activeGroup === index ? 'border-blue-700 bg-blue-700 text-white' : 'border-blue-300 bg-white text-blue-800'}`}>3{index === 0 ? 'A' : 'B'}</button>)}</nav><section className="rounded-3xl border border-slate-200 bg-slate-50 p-4"><h4 className="mb-4 text-lg font-black text-slate-950">Part 3{activeGroup === 0 ? 'A' : 'B'}</h4>{activeGroup === 0 ? <ChoicePart unit={unit} results={results} showPrompt imageTop hideExamples /> : <LetterPart unit={unit} results={results} />}</section><div className="flex justify-between gap-3"><button type="button" disabled={activeGroup === 0} onClick={() => setActiveGroup(0)} className="rounded-xl border-2 border-blue-600 bg-white px-4 py-2 font-black text-blue-800 disabled:opacity-35">← Trang 3A</button><button type="button" disabled={activeGroup >= Math.min(1, units.length - 1)} onClick={() => setActiveGroup(1)} className="rounded-xl border-2 border-blue-700 bg-blue-700 px-4 py-2 font-black text-white disabled:opacity-35">Trang 3B →</button></div></div>;
}

function ReviewPart({ part, results }: { part: ExamPartContent; results: ExamQuestionResult[] }) {
  const units = examPartUnits(part);
  let body: ReactNode;
  if (part.part === 1) body = <LetterPart unit={units[0] || part} results={results} />;
  else if (part.part === 2) body = <ChoicePart unit={units[0] || part} results={results} showPrompt withoutImage />;
  else if (part.part === 5) body = <ChoicePart unit={units[0] || part} results={results} imageTop hideExamples />;
  else if (part.part === 3) body = <CompoundReview units={units} results={results} />;
  else if (part.part === 4) body = <ChoicePart unit={units[0] || part} results={results} showPrompt imageTop hideExamples />;
  else if (part.part === 7) body = <TextPart unit={units[0] || part} results={results} showPrompt={false} split />;
  else if (part.part === 9) body = <WritingPart unit={units[0] || part} results={results} />;
  else body = <TextPart unit={units[0] || part} results={results} />;
  return <section><div className="mb-5"><p className="text-xs font-black uppercase text-indigo-700">Part {part.part}</p><h3 className="mt-1 text-2xl font-black text-slate-950">{part.title}</h3><p className="mt-2 text-sm font-semibold text-slate-700">{part.instruction}</p></div>{body}</section>;
}

function DetailedReview({ playable, review, onBack }: { playable: ExamPlayableSet; review: ExamAttemptReview; onBack: () => void }) {
  const [activePart, setActivePart] = useState(playable.content.parts[0]?.part || 1);
  const partIndex = Math.max(0, playable.content.parts.findIndex(part => part.part === activePart));
  const part = playable.content.parts[partIndex] || playable.content.parts[0];
  const results = review.questions.filter(question => question.part === part.part).sort((left, right) => left.number - right.number);
  const summary = useMemo(() => ({ correct: results.filter(item => item.correct).length, incorrect: results.filter(item => item.correct === false && !item.unanswered).length, unanswered: results.filter(item => item.unanswered).length }), [results]);
  return <main id="ket-reading-writing-review-screen" className="flex min-h-screen items-center justify-center bg-gradient-to-b from-indigo-100 via-white to-sky-50 p-3"><div className="flex max-h-[calc(100vh-1.5rem)] w-full max-w-7xl flex-col overflow-hidden rounded-[2rem] border-4 border-white bg-white shadow-2xl"><header className="shrink-0 border-b border-slate-200 px-5 py-4"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.16em] text-blue-700">Đáp án sau khi nộp</p><h1 className="mt-1 text-2xl font-black text-slate-950">Kết quả chi tiết</h1></div><div className="rounded-2xl bg-blue-100 px-5 py-2"><span className="text-3xl font-black text-blue-800">{review.attempt.score}</span><span className="font-black text-slate-600">/100</span></div></div></header><div className="min-h-0 flex-1 space-y-4 overflow-y-auto bg-slate-50 p-4 sm:p-6"><nav className="sticky top-0 z-40 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-slate-300 bg-white p-3 shadow-sm"><div className="flex flex-wrap gap-2" role="tablist">{playable.content.parts.map(candidate => <button key={candidate.part} type="button" role="tab" aria-selected={candidate.part === activePart} onClick={() => setActivePart(candidate.part)} data-active={candidate.part === activePart} className={`h-10 min-w-16 rounded-full border px-3 text-sm font-black ${candidate.part === activePart ? 'border-blue-800 bg-blue-800 text-white' : 'border-blue-300 bg-white text-blue-900'}`}>Part {candidate.part}</button>)}</div><p className="text-xs font-black"><span className="text-emerald-800">{summary.correct} đúng</span> · <span className="text-rose-800">{summary.incorrect} sai</span> · <span className="text-amber-800">{summary.unanswered} bỏ trống</span></p></nav><div className="relative px-10 sm:px-16"><button type="button" disabled={partIndex === 0} onClick={() => setActivePart(playable.content.parts[partIndex - 1]?.part || activePart)} className="absolute left-0 top-1/2 z-30 flex h-12 w-12 -translate-y-1/2 items-center justify-center rounded-full border-2 border-blue-700 bg-white text-blue-800 disabled:border-slate-300 disabled:text-slate-400" aria-label="Part trước"><ChevronLeft size={28} /></button><ReviewPart part={part} results={results} /><button type="button" disabled={partIndex === playable.content.parts.length - 1} onClick={() => setActivePart(playable.content.parts[partIndex + 1]?.part || activePart)} className="absolute right-0 top-1/2 z-30 flex h-12 w-12 -translate-y-1/2 items-center justify-center rounded-full border-2 border-blue-700 bg-white text-blue-800 disabled:border-slate-300 disabled:text-slate-400" aria-label="Part sau"><ChevronRight size={28} /></button></div></div><footer className="shrink-0 border-t border-slate-200 bg-white p-4"><button type="button" onClick={onBack} className="w-full rounded-2xl border-2 border-blue-700 bg-white py-3 font-black text-blue-800"><ChevronLeft size={16} className="mr-2 inline" />Quay lại tổng kết</button></footer></div></main>;
}

export default function KetReadingWritingResult({ result, review, playable, reviewLoading, error, onReview, onRetry, onBack }: { result: ExamCompletedAttempt; review: ExamAttemptReview | null; playable: ExamPlayableSet; reviewLoading: boolean; error: string; onReview: () => void; onRetry: () => void; onBack: () => void }) {
  const [showReview, setShowReview] = useState(false);
  if (showReview && review) return <DetailedReview playable={playable} review={review} onBack={() => setShowReview(false)} />;
  const pending = result.status === 'pending_review';
  return <main id="ket-reading-writing-result-screen" className="flex min-h-screen items-center justify-center bg-gradient-to-b from-indigo-100 to-sky-100 p-4"><div className="w-full max-w-xl rounded-[2rem] border-4 border-white bg-white p-8 text-center shadow-2xl">{pending ? <FileClock size={64} className="mx-auto text-violet-700" /> : <Trophy size={64} className="mx-auto text-amber-500" />}<p className="mt-3 text-xs font-black uppercase tracking-[.2em] text-blue-700">{pending ? 'Đã lưu bài · Writing đang chờ chấm' : 'Đã nộp bài thành công'}</p>{pending ? <><h1 className="mt-3 text-3xl font-black text-violet-900">Điểm tổng đang được hoàn thiện</h1><p className="mt-3 text-sm font-bold leading-6 text-slate-700">{result.aiGradingMessage || 'Bài Part 9 đã được lưu. Giáo viên có thể thử chấm tự động lại hoặc chấm tay.'}</p><p className="mt-3 text-xl font-black text-violet-800">Điểm khách quan tạm thời: {result.objectiveScore}/100</p></> : <h1 className="mt-2 text-5xl font-black text-slate-950">{result.score}<span className="text-xl text-slate-600">/100</span></h1>}<div className="mt-6 grid grid-cols-3 gap-2"><div className="rounded-2xl bg-emerald-100 p-3"><p className="text-2xl font-black text-emerald-800">{result.correctCount}</p><p className="text-xs font-bold text-emerald-800">Đúng</p></div><div className="rounded-2xl bg-rose-100 p-3"><p className="text-2xl font-black text-rose-800">{result.incorrectCount}</p><p className="text-xs font-bold text-rose-800">Sai</p></div><div className="rounded-2xl bg-amber-100 p-3"><p className="text-2xl font-black text-amber-800">{result.unansweredCount}</p><p className="text-xs font-bold text-amber-800">Bỏ trống</p></div></div>{error && <p className="mt-4 text-sm font-bold text-rose-700">{error}</p>}<div className="mt-7 grid gap-3"><button type="button" onClick={onBack} className="w-full rounded-2xl bg-blue-800 py-3 font-black text-white"><Home size={16} className="mr-2 inline" />Về trang chủ</button>{!pending && playable.content.showReviewAfterSubmit && <button type="button" disabled={reviewLoading} onClick={() => { setShowReview(true); if (!review) onReview(); }} className="w-full rounded-2xl border-2 border-blue-700 bg-white py-3 font-black text-blue-800 disabled:opacity-60">{reviewLoading ? <LoaderCircle size={16} className="mr-2 inline animate-spin" /> : <Eye size={16} className="mr-2 inline" />}Xem kết quả</button>}<button type="button" onClick={onRetry} className="w-full rounded-2xl bg-emerald-700 py-3 font-black text-white"><RotateCcw size={16} className="mr-2 inline" />Làm lại</button></div></div></main>;
}
